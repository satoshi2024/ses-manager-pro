# Source Matrix と法人別36協定 棚卸し（トラックB1 / T067）

- 出所: `attendance-leave-overtime-compliance` task 0（T067）
- 実行基線: `.kiro/audits/2026-08-01-attendance-parallel-track-plan.md` §4 トラックB1、§5 開始条件、§8.4 開工対話
- 本書はproduction codeを1行も変更しない。成果物は本文書のみ。
- 参照した確定文書: `requirements.md`、`design.md`、`overtime-rules.md`（いずれも実行基線 `5e29f39`）
- **時間外の計算ルール自体（値・境界・優先順位）は `overtime-rules.md` で確定済みであり、本書では決め直さない。**
  本書が集めるのは、その確定ルールへ流し込む法人別データである。

---

## 0. 着手前検証（実行基線 §1）の結果

T067着手時の `main` / `origin/main` HEAD = `5e29f39c96da85b29a0fe881326d979896a595d0`。

| 検証項目 | 結果 |
|---|---|
| (2) 17spec台帳の状態 | attendance = `IN PROGRESS`、T067完了・T068〜T074未着手。中央台帳と同期予定 |
| (3) 実適用済みmigration最新 | `V81`（order acceptance remediation）。`V82`/`V83`は未作成 |
| (4) 先行spec | order-acceptance-workflow = `PASS`、merge済み。T067の先行条件を充足 |
| (5) 採番予約 | attendanceは`V83`予約。実適用最新V81で衝突なし。V59/V72は永久欠番 |
| §5 共通開始条件 | 全て充足（production code変更なし、migration新規作成なし、G6決定済み） |
| §5 B1固有条件 | production code変更なし（本書のみ更新） |
| §6 停止条件 | 該当なし（DDL不要、触ってよいファイル外の変更不要。法人別資料未入手は本番release/M gate） |

**矛盾は検出されなかったため、着手する。**

---

## 1. Source of Truth Matrix

### 1.1 結論（最重要境界）

> **雇用勤怠の唯一の正は本システム（F1で新設する `t_employee_attendance` / `t_attendance_month`）である。
> `t_work_record_daily` / `t_work_record`（客先請求工数）は請求のための別sourceであり、
> 雇用上の勤怠の正としては扱わない。freeeはdownstream（照合先）であり、正ではない。**

これは `requirements.md` 冒頭の「最重要境界」および design.md §3 の`AttendanceProvider`（Internal/Freee、
本システムを正としread-only照合）と一致し、G6決定（design.md §5.2冒頭に明記）を追認するものである。
本書はこの決定を再確認しただけであり、新たな決定は行っていない。

### 1.2 各sourceの実体と境界

| source | 実体（現行コード上の根拠） | 正/非正 | 用途 | 雇用勤怠との関係 |
|---|---|---|---|---|
| 本システム（雇用勤怠、F1で新設） | `t_employee_attendance`, `t_attendance_month`（design.md §1、未実装） | **正** | 出退勤・休憩・法定内外時間・休日/深夜・勤務区分・勤務地の唯一の記録先 | — |
| `t_work_record`（月次実績工数） | `V5__create_work_record_billing.sql:1-16`。`contract_id`単位、`actual_hours`（DECIMAL(5,1)）のみ、出退勤・休憩・深夜/休日区分を持たない | **非正（請求source）** | 契約の月次請求・支払金額算定 | 雇用勤怠と一致しない。差異は`R4`で比較表示するのみで、雇用勤怠を上書きしない |
| `t_work_record_daily`（日次入力） | `V32__engineer_self_service.sql:23-36`。`work_record_id`（＝契約）に紐づき`engineer_id`列を持たない。`worked_hours`は自動計算値の保存であり出退勤の生ログではない | **非正（請求sourceの内訳）** | マイ勤怠画面での日次入力、月次実績工数の内訳 | 同上。**エンジニア単位ではなく契約単位のレコード**である点も雇用勤怠と構造的に異なる（1エンジニアが複数契約を掛け持つケースを想定していない設計） |
| freee（給与・勤怠連携） | `t_freee_connection`, `t_freee_employee_link`（`V21__freee_payroll_integration.sql`）。現在は勤怠でなく給与連携のみ | **非正（downstream）** | 給与計算への連携 | tasks.md B1（freee/provider sync）で本システムの確定済み雇用勤怠をfreeeへ送信する片方向連携になる予定（design.md §3の`AttendanceProvider`基盤を利用）。freee側の値で本システムを上書きしない（design §5.4） |

### 1.3 なぜ両者が一致しないか（HR確認用の要点）

- `t_work_record_daily`は契約(`work_record_id`)単位のレコードであり、`engineer_id`を直接持たない。複数契約を掛け持つ要員や、契約外の社内業務時間は表現できない。
- `worked_hours`は始業/終業からの自動計算値の保存であり、休憩の実績・深夜/法定休日区分・所定外/所定内の区別を持たない（`break_minutes`はあるが、それ以外の勤怠区分列が存在しない）。
- 客先への請求は契約の精算幅（例: 140h〜180hのような幅精算）で丸められることが多く、実際の出退勤時間と請求時間は制度上一致しない。
- 結論として、`t_work_record_daily`／`t_work_record`を雇用勤怠の代用データとして流用することはできない。F1は独立した新テーブル群を必要とする（design.md §1のとおり）。

---

## 2. 勤務区分（work_type）棚卸し

`t_employee_attendance.work_type`（design.md §1）に入れる値の候補一覧。**現行システムに勤務区分の概念は存在しない**
（`t_engineer`・`t_work_record`・`t_work_record_daily`のいずれにも勤務区分列がない）ため、以下はSES業態の一般的な
区分から起こしたドラフトであり、**HR/就業規則側の確認が必要**（fail-closedの対象ではないが、確定するまでF1のENUM値
として固定しない）。

| 候補コード | 名称 | 想定用途 | 確認状況 |
|---|---|---|---|
| `normal` | 通常勤務（原則の労働時間制） | 自社オフィス勤務・裁量なしの客先常駐 | **未確認**（就業規則要参照） |
| `deemed_outside` | 事業場外みなし労働時間制 | 客先常駐で使用者の具体的指揮監督が及ばない形態のSESエンジニア | **未確認**。適用の有無自体が法人・契約形態次第 |
| `flex` | フレックスタイム制 | 一部管理部門・エンジニアで採用している可能性 | **未確認** |
| `remote` | 在宅勤務 | リモート案件 | **未確認**。`workplace_type`（design §1）と役割分担が必要 |
| `discretionary` | 専門業務型裁量労働制 | 該当職種がいるかどうか | **未確認**。対象業務がSESの一般的なエンジニア職に該当するかはHR/社労士確認が必要（安易に適用しない） |

**finding**: 勤務区分の実際の運用（どの区分を採用しているか、法人ごとに異なるか）はHR未確認。F1のDDLで
`work_type`をENUM等に固定する前に、上記候補を実際の就業規則と突合すること。

---

## 3. カレンダー（法定休日の曜日）棚卸し

design.md §1の`m_work_calendar` / `m_work_calendar_day`、および `overtime-rules.md` §1.2 の定義
（「法定休日の曜日は`m_work_calendar_day.type`で表現し、カレンダー側で持つ」）に対応する棚卸し。

**現行システムに祝日・所定休日・法定休日の概念は一切存在しない**（`祝日`/`holiday`/`calendar`/`休暇`の
キーワードでJava/migration全体を検索して該当ゼロを確認済み）。したがって以下は全法人**未確認**である。

| 法人 | 週休制（推定） | 法定休日の曜日 | 所定休日の曜日 | 確認状況 |
|---|---|---|---|---|
| （§5の法人一覧の全行） | 週休2日制と推定 | **未確認** | **未確認** | **全法人で未確認**。就業規則の休日規定を個別に参照する必要がある |

> **法定休日の曜日が全法人分そろっているか: そろっていない（未確認）。**
> `overtime-rules.md` §1.2 のとおり、法定休日労働とそれ以外の所定休日労働はルール1/2/3への算入可否が異なるため、
> この列が未確定のままではF1のcalculator入力（`overtimeMinutes`か`totalMinutes`か、design §5.2の3点目）を
> 法人別に正しく組み立てられない。F1はスキーマと未確定値をfail-closedで扱う構造を先行実装できるが、
> HR確認が完了するまで法人別カレンダーの確定値をseedせず、適合判定を本番締めへ開放しない。

---

## 4. 休暇種別棚卸し

`requirements.md` R2.1（有給/半休/時間休/代休/欠勤/特別休暇）に対応する種別一覧。

| 種別 | 本システムでの管理方針（design.md §5.1準拠） | 残数の正 | 確認状況 |
|---|---|---|---|
| 有給休暇 | `t_leave_request` | 外部人事システムが正なら参照表示のみ、本システムが正なら`t_leave_ledger`で付与/消化管理 | **未確認**：現状どちらが正かHR未確認（現行システムに休暇残数テーブルは存在しない） |
| 半休 | 同上（`requested_minutes`で分単位表現） | 同上 | 同上 |
| 時間休 | 同上 | 同上 | 同上 |
| 代休 | 同上。法定休日労働の代替か所定休日労働の代替かで扱いが変わりうる | 本システム管理が前提（外部システム側が代休を扱わない可能性） | **未確認** |
| 欠勤 | 同上（無給） | — | — |
| 特別休暇 | 同上。慶弔等、法人ごとに制度が異なりうる | 本システム管理が前提 | **未確認**：法人別に制度差異があるかHR未確認 |

**finding**: 休暇残数の正（本システム／外部人事システムのどちら）が全種別・全法人で未確認。
design.md §5.1 は「本システムが正の場合のみ台帳を持つ」としており、外部が正の種別についてはR2.2のとおり
残数不足でも申請を拒否しない仕様になる。この判定はHR確認結果次第でF1〜A2の実装に影響するため、
A2着手前に確定させる必要がある（本書のスコープ外、F1/A2への申し送り）。

---

## 5. 法人別36協定内容一覧

### 5.1 前提：法人マスタが未実装であることの明記（最重要finding）

`m_legal_entity`（法人マスタ）は**現行システムに存在しない**。根拠:

- `multi-company-tenant-isolation`（spec #1）R1.1が`m_tenant`/`m_legal_entity`の新設を定義しているが、
  同specは2026-07-26の発注者決定により「顧客ごとの独立DB」を正式採用し、T002以降（`m_legal_entity`の実装を含む）は
  **DEFERRED**（`git show origin/main:.kiro/specs/multi-company-tenant-isolation/requirements.md`
  「G0決定後の現行適用範囲」節）。
- 既存の`legal_entity_id`列（`m_organization_unit`, `m_cost_center`: `V1__create_tables.sql:442,510`）は
  いずれも`COMMENT '法人ID（法人マスタ実装前は外部参照値）'`と明記された**未実装のプレースホルダ**であり、
  参照先マスタも既定値seedも持たない（`V60`のLEGACY組織backfillでも`legal_entity_id`は`NULL`のまま）。
- `t_freee_connection`（`V21__freee_payroll_integration.sql`）は単一のfreee接続のみを前提にした構造であり、
  `tenant-inventory.md`も「現在はglobal単一接続」と記録している。
- `m_system_config`の`company_name`（`V2__init_master_data.sql:185`、値`SES Manager Pro`）は表示用の単一文字列であり、
  複数法人を列挙する構造を持たない。

**したがって、本システムのデータからは「この会社が何法人で構成されているか」自体を判定できない。**
これは`overtime-rules.md`§3の「`m_overtime_agreement`が存在しない法人は判定不能としてfindingを出す」の対象以前の
問題であり、**法人の一覧そのものをHRから提示してもらう必要がある。**

### 5.2 法人別36協定内容一覧（テンプレート）

以下はHRへ提示し埋めてもらうための表である。列構成は`overtime-rules.md`§3（優先順位1: `m_overtime_agreement`の
法人別実協定）およびdesign.md §1の`m_overtime_agreement(legal_entity_id, valid_from/to, special_clause, limits/config)`
に対応させてある。**行を埋めずに「適合」や既定値を仮置きしない**（`overtime-rules.md`§3の fail-closed 原則）。

| 法人名 | 協定の有無 | 協定書の入手状況 | 特別条項の有無 | 月上限 | 年上限 | 起算日（協定年度の開始月） | 法定休日の曜日 | 備考 / finding |
|---|---|---|---|---|---|---|---|---|
| （システム上で確認できる唯一の会社名: `SES Manager Pro`。実際の法人単位への分解はHR未確認） | **未確認** | **未入手** | **未確認** | **未確認** | **未確認** | **未確認** | **未確認** | 上記5.1のとおり法人マスタ自体が未実装。実際に複数法人（例: 派遣元許可法人と請負契約法人を分けている等）で運営されている場合、この1行では足りない。HRに法人の実数と名称一覧の提示を依頼すること |

**この表がF1に対して意味すること**: 現時点で`m_overtime_agreement`へ登録できる行はゼロである。
`overtime-rules.md`§3の解決順（1. 法人別協定 → 2. `m_system_config`のシステム既定 → 3. コード定数）に従えば、
HR確認が完了するまでは全法人が第2段（システム既定）または第3段（コード定数=Tier A値）で暫定判定され、
**判定不能のfindingとして扱う**べきであり、「協定なし＝適合」と扱ってはならない。これはF1/F2の
着手を止める条件ではなく、判定結果と本番締めをfail-closedにする条件である。

### 5.3 HRへの依頼事項（本書の主目的）

1. この会社（テナント）が実際に何法人で構成されているか（1法人のみか、複数法人か）の一覧。
2. 各法人について、36協定の締結有無。
3. 締結ありの場合、協定書の写し（特別条項の有無、月/年上限、協定年度の起算月）。
4. 各法人の就業規則における法定休日の曜日（例: 日曜固定か、シフト制で変動するか）。
5. 各法人の管理監督者（適用除外者）に該当する役職・現在の該当者一覧（§6参照）。

---

## 6. 適用除外者（管理監督者）一覧

### 6.1 現状

`overtime-rules.md`§2 #14「管理監督者・適用除外者はルール1〜6の対象外（労基法41条2号）」に対応する
区分フラグは、**`t_engineer`はじめ既存のどのテーブルにも存在しない**
（`t_engineer`のカラムは`V1__create_tables.sql:85-121`で全列確認済み。役職に相当する列は
`t_user_organization.position_name`（`V60__organization_management_accounting.sql:47`）のみで、
これは自由入力のVARCHARであり、管理監督者判定に使える構造化フラグではない）。

### 6.2 適用除外者一覧（テンプレート）

| 対象者 | 所属法人 | 役職（`position_name`相当） | 管理監督者に該当するか | 確認状況 |
|---|---|---|---|---|
| （HRから個別に提示してもらう） | — | — | — | **未確認**。現行システムに該当者を一意に特定する手段がない |

**finding**: 適用除外者の判定を「役職名の文字列パターンマッチ」等で自動化しない。労基法41条2号の
管理監督者該当性は役職名だけで機械的に決まらない（職務内容・責任と権限・待遇の実態を要件とする）ため、
HRが個別に確定した対象者リストをそのまま`t_engineer`側の新設フラグ（F1で追加、design.md §1参照）へ
反映する運用とする。

---

## 7. Findings 総括

| # | finding | 影響先 | 対応 |
|---|---|---|---|
| F-1 | `m_legal_entity`（法人マスタ）が未実装。法人の実数・名称一覧が本システムのデータから判定不能 | 5章 全体、F1のDDL・seed | HRに法人一覧の提示を依頼（5.3） |
| F-2 | 全法人で36協定書が未入手（協定の有無すら未確認） | F1の`m_overtime_agreement` seed、F2の判定結果 | HR/各法人へ照会。入手まで判定不能findingとして保持し、F1/F2の開発は継続 |
| F-3 | 法定休日の曜日が全法人で未確認 | F2のcalculator入力、`m_work_calendar_day.type` | 就業規則から確認。確認前は法人別カレンダーを確定せず、本番締めを開放しない |
| F-4 | 勤務区分（work_type）の実運用がHR未確認 | F1の勤務区分モデル | 就業規則突合。未確認値を法人の既定値にしない |
| F-5 | 休暇残数の正（本システム/外部）が全種別・全法人で未確認 | A2の実装分岐（4章） | HR確認 |
| F-6 | 適用除外者（管理監督者）を特定する構造化フラグが存在しない。対象者一覧も未確認 | F1のフラグ追加、F2の非判定処理（6章） | HRから個別リスト取得 |

**いずれのfindingも「未確認＝適合」として扱ってはならない。** `overtime-rules.md`§3の fail-closed 原則により、
F1/F2は上記が確定するまで該当法人・該当者を「判定不能」として扱う実装にする。

---

## 8. テスト要件（L0）充足確認

実行基線§8.4「テスト要件: L0」に対応する自己点検。

- [x] 法人ごとに協定の有無と入手状況が記録されている（5.2の表。現状は「未確認/未入手」で統一されているが、
      これは実データが無いためであり、列自体は法人ごとに記録する構造になっている）
- [x] 法定休日の曜日が全法人分そろっているか未確認と明記（3章、5.2表）
- [x] source matrixが既存work recordとの境界を含む（1章）
- [x] `git diff --check` exit 0 — `git add`後の`git diff --cached --check`で確認済み（exit 0）

---

## 9. F1（DDL, V83）への申し送り

- 本書5.1のとおり、`m_overtime_agreement`は現時点で挿入できる法人別行を持たない。V83のseedは
  `overtime.*`のシステム既定値（`overtime-rules.md`§1/§2のconfig key、design.md F1ガイダンス）のみとし、
  法人別行の投入は本書5.2〜5.3のHR確認が完了してから別途行う。
- `work_type`・法定休日曜日・休暇残数の正・適用除外者フラグは、いずれも値/運用が未確定（2, 3, 4, 6章）。
  F1のDDL自体（カラム定義）は`design.md`§1の型（分の整数、NULL/0の区別等）に従って進めてよいが、
  ENUM値や既定値の**中身**を本書の確認未了のまま確定しないこと。
- 本書は`overtime-rules.md`の値・境界・優先順位を一切変更しない。F1/F2の実装は同書をそのまま参照する。

## 10. 開発着手と本番release gateの区分

- G6と`overtime-rules.md`の確定値により、T067完了後のF1/F2実装は開始できる。法人別36協定・就業規則・
  社労士確認が未入手であることを理由に、開発を停止しない。
- ただし、協定行がない法人はcalculatorで判定不能findingを出し、既定値で適合にしない。法人別休日・勤務区分・
  適用除外者が未確認のまま、判定結果を本番締めの適合証明として扱わない。
- HR/発注者の確認結果、法人別協定の登録、法定休日曜日、適用除外者の確定、社労士Reviewは、T074/Mの自己PASSではなく
  本番release gateとして別管理する。
