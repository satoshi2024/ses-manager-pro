# dev プロファイル専用設定。
# R3_SCALE_300 シード（V100の基礎データ + V134/V135の現行module補助データ）を
# 通常起動（spring.profiles.active=dev）でも自動適用する。
# 他のプロファイル（test/prod）には影響しない。
spring:
  flyway:
    locations: classpath:db/migration,classpath:db/migration-dev
