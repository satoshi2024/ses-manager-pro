#!/usr/bin/env bash
# HFP-03 unit test 用の fake mysqlbinlog。
set -uo pipefail

if [[ -n "${FAKE_ARGV_LOG:-}" ]]; then
  printf '%s\n' "$*" >> "$FAKE_ARGV_LOG"
fi

for arg in "$@"; do
  if [[ "$arg" == "--version" ]]; then
    printf '%s\n' "${FAKE_MYSQLBINLOG_VERSION_LINE:-mysqlbinlog  Ver 8.0.36 for Linux on x86_64 (MySQL Community Server - GPL)}"
    exit 0
  fi
done

exit 0
